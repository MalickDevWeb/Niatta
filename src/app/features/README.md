# Feature boundaries

Create one route-owned folder per domain: `home`, `products`, `stores`, `map`, `reports`,
`auth`, `profile`, and `admin`. Shared UI belongs in `src/app/shared`; authentication,
HTTP, models, and guards belong in `src/app/core`.
